---
title: Codex
description: Local per-call routing and Codex Action's single-session mode.
---

Codex supports two ways to run woostack:

- Local Codex can select a model for each worker. Woostack passes the model and, for GPT-5-family
  reasoning models, a separate `reasoning_effort` value for each task.
- A Codex Action job runs in one session. Woostack selects one model before the job starts, and all
  task tiers use it.

For local workers, woostack passes the working directory when the runtime supports it. The task
prompt always names the exact worktree, the separate checkout assigned to the task.

If the provider's usage limit is exhausted, the worker returns an error. Configured fallback
entries give you a manual preference order; woostack does not automatically try each model when
starting a worker.

For detection, local and Action behavior, per-skill instructions, and fallback rules,
read the [canonical Codex reference](https://github.com/howarewoo/woostack/blob/main/skills/using-woostack/references/hosts/codex.md).
